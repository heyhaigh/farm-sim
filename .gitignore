# macOS
.DS_Store

# editor
.vscode/
.idea/

# scratch / screenshots
*.log
screenshots/
.playwright-mcp/

# node (only if tooling is added later)
node_modules/

# licensed reference art (CraftPix) used as inspiration only + workflow scratch
assets/

# local LLM key for the expressive channel (server.mjs) - NEVER commit
.env

# SuperMemory local self-hosted store (encrypted data + embedding models)
.supermemory/

# 1-bit icon reference pack (browsed in component-library/index.html) — NOT a runtime
# dependency of the game; 1.6k reference PNGs that don't belong in the shipped repo
component-library/

# personal Claude Code skill/config files — never belong in the project repo
.agents/
.claude/
skills-lock.json

# council/kimi/review scratch output + review packets (workflow scratch, not deliverables)
.council-*.md
KIMI_REVIEW_PACKET.md

# generated Codex review patch (scratch, not a deliverable)
review.diff
